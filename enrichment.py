# This module has been removed.
# MAC address enrichment is no longer handled here.
# Vendor resolution uses the IEEE OUI lookup in oui.py.
# External URLs can be set directly on dcim.mac_addresses via the
# 'external_url' custom field in NetBox.
